[project]
name = "vrptw-demo"
version = "0.1.0"
description = "Hybrid solver for Vehicle Routing Problem with Time Windows (VRPTW) combining Deep Q-Network (DQN) warm-start with Adaptive Large Neighborhood Search (ALNS)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
